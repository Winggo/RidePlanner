<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmps121.rideplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rpe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rpe_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreatingGroups" />
        <!-- <<<<<<< HEAD -->

        <activity android:name=".EditProfile" />
        <activity android:name=".eventDetails" />
        <activity android:name=".viewAllCars" />
        <activity android:name=".yourCar" />
        <activity android:name=".finalCarScreen" />
        <!-- ======= -->
        <!-- &gt;>>>>>> dev -->
        <activity android:name=".JoiningGroups" />
        <activity android:name=".ViewGroups" />
        <activity android:name=".GroupPage"></activity>
    </application>

</manifest>